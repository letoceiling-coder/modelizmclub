/**
 * Вкладки комнаты направления.
 *
 * Отдельным модулем, а не рядом с `SubcategoryRoomPage`, намеренно. Маршрут
 * `categories.$id.index` проверяет `?tab=` в `validateSearch`, а эта часть
 * маршрута в отдельный чанк не выносится — она нужна роутеру до загрузки
 * компонента. Пока константа жила в модуле страницы, импорт одного массива
 * из четырёх строк затаскивал в главный чанк всю страницу комнаты: 28 КБ,
 * замер карты исходников 11.09.
 *
 * «Записи» стоят первыми и открываются по умолчанию. Панель справа даёт две
 * двери в комнату — название и значок чата, — и пока обе вели на чат, вторая
 * ничего не добавляла. Теперь название открывает записи, значок — чат.
 */
export type RoomTab = "posts" | "chat" | "ads" | "members";

export const ROOM_TABS: readonly RoomTab[] = ["posts", "chat", "ads", "members"];
