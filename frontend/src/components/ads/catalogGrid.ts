/**
 * Сетка каталога — по ширине самой сетки, а не по брейкпоинту экрана.
 *
 * Минимум колонки: 220 px на широких сетках и меньше на узких —
 * `min(220px, 120px + 13%)`, проценты от ширины сетки. Одно число не
 * даёт нужных рядов: на 768 сетка 656 px и трём карточкам нужен минимум
 * не больше 210,7, на 1024 сетка 888 px и четвёртая карточка не должна
 * влезать — минимум больше 213. Замер формулы в Chromium, 11.09:
 * 351 → 2, 656 → 3, 888 → 3, 970 → 4 колонки (375, 768, 1024, 1440
 * со свёрнутым меню и фильтрами слева).
 *
 * Строка целиком — Tailwind находит классы по исходнику; подчёркивания в
 * calc() он превращает в пробелы.
 */
export const CATALOG_GRID =
  "grid gap-[12px] [grid-template-columns:repeat(auto-fill,minmax(min(220px,calc(120px_+_13%)),1fr))]";
