# Hero videos

Drop the landing hero videos here (optional — the hero falls back to
`src/assets/cover-modelizm.jpg` when they are absent):

- `modelizm-hero-desktop.mp4` — horizontal, shown on ≥768px
- `modelizm-hero-mobile.mp4` — vertical, shown on <768px

Recommended: H.264 MP4, muted, looped, ~8–15s, ≤6 MB.
