# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output

# Generated by the TanStack Router plugin on every build. Tracking it produced a
# permanently dirty working tree on the server (the generator reorders entries),
# which is exactly what the deploy's dirty-tree guard has to ignore.
src/routeTree.gen.ts
.output.legacy-*
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gstack/
