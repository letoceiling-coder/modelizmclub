# PHP-FPM pool counters every five minutes. The log is the record; a rise in
# "max children reached" also raises an alert (see /usr/local/bin/fpm-watch.sh).
*/5 * * * * root /usr/local/bin/fpm-watch.sh >/dev/null 2>&1
