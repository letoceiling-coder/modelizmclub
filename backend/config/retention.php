<?php

/*
|--------------------------------------------------------------------------
| Сроки хранения журналов
|--------------------------------------------------------------------------
|
| Две таблицы пишутся на каждое действие пользователя и до 11.09 не
| чистились никогда: client_logs — 2217 строк с 17.07, banner_events — 800
| с 02.09. На нынешнем трафике это мегабайты, на тысяче пользователей —
| гигабайты, причём растущие линейно и молча.
|
| Сроки разные, потому что назначение разное.
|
| client_logs — разбор инцидентов. Полезны, пока инцидент свежий: жалоба
| приходит в тот же день или на следующий, а через месяц по записи о
| качестве связи уже ничего не восстановить. Месяц.
|
| banner_events — сырые показы и клики. Итоги живут не здесь: баннер хранит
| impressions_count и clicks_count у себя, и админка показывает именно их.
| Сырые строки нужны только для разбивки по дням, и она переживает уборку —
| перед удалением строки сворачиваются в banner_event_daily. Три месяца
| сырых, дневные итоги — навсегда.
|
| audit_logs намеренно не здесь: журнал решений должен жить долго, это его
| назначение.
|
*/

return [
    'client_logs_days' => (int) env('RETENTION_CLIENT_LOGS_DAYS', 30),
    'banner_events_days' => (int) env('RETENTION_BANNER_EVENTS_DAYS', 90),
];
