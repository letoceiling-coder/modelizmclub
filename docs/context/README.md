# Пакет контекста — снимок 03.09.2026

Девять файлов `10-tree` … `80-payments` собраны одним проходом по
`origin/master` @ `ecb4d60` (03.09.2026). Они описывают состояние на тот
день и **с тех пор не обновлялись**.

К 10.09 между снимком и деревом — **520 коммитов и 835 изменённых файлов**.
За эту неделю сделаны выравнивание направлений, кошелёк и платежи, отказ от
трёх языков в пользу одного, ворота CI, переезд просмотрщика записи. Пакет
всего этого не знает.

**Читайте его как историю, а не как описание сегодняшнего дня.** Устаревшая
запись хуже отсутствующей: она не заставляет пойти и проверить.

## Что в пакете заведомо неверно

Проверено 10.09.2026 — по дереву и по проду. Список не полный: это то, на
чём споткнулись, а не результат сплошной сверки.

| Где | Написано | На самом деле |
| --- | --- | --- |
| `70-perf-facts.md` | «Сжатие на сервере не включено», `index-*.js` отдаётся 2 320 098 байт без сжатия | Сжатие включено: тот же чанк приходит `content-encoding: br`, 340 983 байта; HTML — `gzip` |
| `10-tree.md` | «Три файла локализации — 10 160 строк»: `en.ts` 3446, `zh.ts` 3401, `ru.ts` 3313 | Английский и китайский словари удалены 10.09. Осталось два файла: `ru.ts` 2624 и `ru-admin.ts` 1204 |
| `10-tree.md` | «В корне `.github/` нет», workflow-файлы инертны, «CI фактически не запускался ни разу» | `.github/workflows/ci.yml` лежит в корне и работает: четыре работы на каждый push, и выкатка без зелёного прогона запрещена правилом в `CLAUDE.md` |
| `10-tree.md` | `lib/mock.ts` — 954 строки | 3231 строка |
| `60-state.md` | `lib/mock.ts` — «2000+ строк моков» | 3231 строка |
| `20-routes-frontend.md` | 75 маршрутов в таблице | 81 файл маршрута в дереве; на срезе их было 76 |

Числа платежей в `80-payments.md` — срез базы на 03.09; на 10.09 в `pending`
31 платёж, а не то, что в таблице.

## Что с этим делать

Три пути, выбирать заказчику:

1. **Оставить как снимок** — так сейчас и сделано: каждый файл начинается
   с даты и ссылки сюда. Дёшево, но пакет продолжает стареть.
2. **Пересобрать** — один проход тем же способом по сегодняшнему `master`.
   День работы, и через неделю всё повторится.
3. **Удалить** — если пакетом никто не пользуется, отсутствие честнее
   снимка недельной давности.
