# Расхождения снимка 03.09 с нынешним поведением

Снимок в `docs/context/` собран 03.09.2026 на `ecb4d60`. Это список
того, где он разошёлся с деревом на 26.09 — **по разделам, без
переписывания**: решение, переписывать пакет или пометить его историей,
за заказчиком.

## Насколько разошёлся

| | |
| --- | --- |
| Коммитов с момента снимка | **1004** |
| Изменённых файлов | **1332** |
| Строк добавлено / удалено | 137 166 / 38 931 |

В README пакета стоит замер от 10.09: 520 коммитов и 835 файлов. За
две с половиной недели расхождение выросло вдвое.

Ниже — по файлам. Числа получены измерением 26.09, а не оценкой.

---

## `10-tree.md` — дерево каталогов

**Расходится умеренно.** Состав верхнего уровня прежний. Появилось
двенадцать рабочих деревьев `моделист-*` внутри репозитория (перенесены
26.09 решением заказчика; в `.git/info/exclude` они скрыты от
`git status`). Появился `deploy/scripts/lib/` — общая библиотека
скриптов, до 26.09 её не было.

## `20-routes-frontend.md` — маршруты интерфейса

**Расходится умеренно.** В снимке 84 строки таблицы, на диске 83 файла
маршрутов — числа близки, но состав другой: страница мероприятия,
страницы админки по вкладкам монетизации (четыре новых `?section=`),
страница спора. Проверять надо не количество, а список.

## `21-routes-api.md` — маршруты API

**Расходится сильно.** В снимке перечислена **21** строка, сейчас
зарегистрировано **482** маршрута. То есть снимок описывает меньше
пяти процентов нынешнего API.

Читать его как справочник нельзя. Живой список:
`php artisan route:list`.

## `30-data-model.md` — модель данных

**Расходится сильно.** В снимке разобрано **14** таблиц и упомянуто «70
остальных моделей». Сейчас в схеме **142** таблицы.

Появилось с тех пор, в том числе: мероприятия и участники
(`club_events`, `club_event_attendees`), отдельные права сотрудников
(`admin_permission_grants`, 26.09), учётные записи приёмки (признак
`users.is_qa_account`), ручное напоминание (`club_events
.manual_reminder_at`).

Эталон схемы `backend/database/schema/objects.txt` актуален всегда —
его пересобирает каждая ветка с миграцией, и это проверяют ворота CI.
**Он и есть настоящая модель данных**, а раздел снимка — её состояние
на 03.09.

## `40-api-contract.md` — договор API

**Расходится сильно, и это опаснее прочего.** Контракт описывает форму
ответов, по которой пишут клиенты. За три недели изменились, как
минимум:

- `offers_cdek` у объявления — теперь означает «предлагает СДЭК», а не
  «ничто не мешает» (правка 25.09);
- ответ списка категорий несёт `posts_count`, `listings_count`,
  `has_admin` (C2);
- карта доступа `GET /admin/access` несёт `granted` — права,
  выданные поверх роли (C3);
- список акций несёт `state`, `seats_left`, `days_left`,
  `days_until_start` (C4);
- объявление несёт автора с рейтингом (использовалось и раньше,
  но в контракте не описано);
- отказ при отправке SMS несёт `code` и `retry_after`, в том числе при
  429 (D3 и починка 26.09).

Живой источник — `php artisan scramble:export` и `docs/API.md`.

## `50-components.md` — компоненты интерфейса

**Расходится умеренно.** Появились `Appear`, `ReducedMotionSwitch`,
`NativeSelect`, `UserAvatar` как единственный аватар приложения,
`RouteErrorState`, `InlineFeedback`. Правило «не заводить второго»
закреплено в `CLAUDE.md` и в правилах линта — в снимке его нет.

## `60-state.md` — состояние

**Расходится слабо.** Устройство прежнее. Добавились правила гидрации
(`useHydrated`, `useHasToken`) — они описаны отдельно в
`docs/hydration.md`, которого на 03.09 не было.

## `70-perf-facts.md` — замеры

**Устарел целиком.** Числа от 03.09: 84 ошибки `tsc`, 8508 замечаний
линтера, крупнейший чанк 668 КБ gzip, тестов фронта нет.

Сейчас: `tsc` — ноль, линтер — ноль ошибок при 1516 предупреждениях,
тестов фронта 386, тестов бэкенда 1186. Ни одно число снимка не
соответствует.

## `80-payments.md` — платежи

**Расходится сильно и в опасную сторону.** Снимок описывает контур на
03.09. С тех пор:

- `BILLING_PROVIDER` переключён со `stub` на `vtb`, **при тестовых
  адресах банка** — разбор в `deploy/docs/vtb-go-live.md`;
- добавлены выплаты продавцам (СБП и карта) отдельной интеграцией;
- появилась надбавка к доставке, вкладки монетизации, сводка
  «принято / выплачено / комиссия»;
- комиссия сделки считается по `safe_deals.platform_fee_kopecks`, а не
  по проводкам кошелька — проводок вида `safe_deal_commission` в базе
  ноль.

Читать этот раздел как описание сегодняшних денег **нельзя**.

---

## Что я предлагаю

Переписывать пакет целиком сейчас не стоит: он потеряет ценность
снимка и не станет надёжнее живых источников. Каждый его раздел уже
имеет живую замену:

| Раздел снимка | Чем заменяется |
| --- | --- |
| Маршруты API | `php artisan route:list` |
| Модель данных | `backend/database/schema/objects.txt` |
| Договор API | `docs/API.md`, Scramble |
| Замеры | ворота CI и замеры в ветках |
| Платежи | `deploy/docs/vtb-go-live.md` |

Предлагаю пометить пакет снимком — дописать дату и строку «состояние на
03.09, живой источник такой-то» в шапку каждого файла — и оставить.
Это дешевле переписывания и честнее: устаревшая запись хуже
отсутствующей ровно тем, что не заставляет пойти и проверить, а ссылка
на живой источник заставляет.

Решение за вами. Если нужно переписать — это отдельная задача, и по
объёму она сравнима с самим пакетом.
