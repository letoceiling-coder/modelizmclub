# Настройки: обход 12.09

Проверено на проде под тестовым подписчиком на 1440 и 375 и по коду
(`frontend/src/routes/settings.*.tsx` и то, что они рисуют).

## Что проверено руками и работает

- Все 13 разделов открываются: ошибок API и консоли нет, горизонтального
  скролла нет.
- Переключатель уведомлений сохраняется после перезагрузки и возвращается
  назад так же.
- Тема переключается и переживает перезагрузку.
- Вкладки кошелька «Кошелёк» / «Платежи» переключаются. Окна «Пополнить» и
  «Вывести» открываются и закрываются по Esc.
- Подтверждения «Отозвать согласие» и «Удалить аккаунт» открываются и
  отменяются.

Не нажималось — шлёт письмо или SMS, списывает деньги, удаляет данные или
выходит на других устройствах: смена email и пароля, SMS, «Подключить MAX»,
«Добавить карту», оплата и вывод, «Сохранить» в реквизитах, «Очистить
историю», «Выйти на других устройствах», «Экспорт моих данных»; «Удалить
аккаунт» — только открыт и отменён. По ним выводы из кода.

## Не работает

1. ~~**Смену email нельзя завершить.**~~ — **починено 12.09** в
   `fix/settings-defects`. Сервер и раньше умел всё; не хватало двух вещей.
   Во-первых, незавершённая смена не отдавалась наружу — добавлено поле
   `pending_email` в `UserResource`, только владельцу (ресурс общий с
   админским списком, и `when` заодно не даёт запросу выполняться на каждой
   строке). Во-вторых, страница подменяла адрес у себя: теперь
   `requestEmailChange` только переводит блок в ожидание, а адрес меняет
   `confirmEmailChange` с кодом из шести цифр, после чего состояние берётся
   заново с сервера. Повторная отправка идёт на ожидающий адрес
   (`account/email/verify/resend`), а прежняя кнопка «Отправить письмо
   подтверждения» осталась там, где ей место, — при неподтверждённом
   **текущем** адресе. Закреплено `tests/Feature/EmailChangeFlowTest.php`
   (5 проверок): запрос не трогает адрес учётки, `pending_email` виден
   владельцу и не виден после истечения, неверный код адрес не меняет.

   Ниже — как было записано.

   **Смену email нельзя завершить.** Сервер хранит новый адрес как
   ожидающий и шлёт на него код; адрес меняется только после
   `POST /account/confirm-email` с кодом. На сайте этот вызов не делается
   нигде, поля для кода нет. Интерфейс сразу показывает новый адрес и пишет
   «Email обновлён — подтвердите по ссылке из письма», хотя в письме код, а
   не ссылка; после перезагрузки возвращается старый. Появившаяся кнопка
   «Отправить письмо подтверждения» шлёт письмо на старый адрес.
   `settings.account.tsx:126-149`; `resendEmailChangeVerification` в
   `lib/api/account.ts:50` есть, но не вызывается.
2. ~~**«Баланс пополнен» без проверки у сервера.**~~ — **починено 12.09**.
   Идентификатор платежа запоминается в `sessionStorage` до ухода на банк
   (`lib/payments/pending-topup.ts`) — собственный возвратный адрес сервера
   его не несёт, потому что строится раньше, чем платёж создан. На возврате
   берётся идентификатор из адреса, а если его нет — запомненный, и статус
   спрашивается у сервера. Спросить не о чем (другая вкладка, очищенное
   хранилище, адрес открыли руками) — показывается «Проверяем платёж у
   банка», а не успех. Проверено на сборке: без запомненного платежа запрос
   не уходит и «Баланс пополнен» не показывается; с запомненным уходит
   `POST /payments/{uuid}/sync` и показывается ответ сервера.

   Ниже — как было записано.

   **«Баланс пополнен» без проверки у сервера.** При возврате с оплаты без
   `uuid` платежа тост об успехе показывается сразу, без `syncPayment`, — а
   сервер по умолчанию возвращает именно без него. `settings.wallet.tsx:238-246`.
3. ~~**Разблокировка может не дойти до сервера.**~~ — **починено 12.09**.
   Числовые идентификаторы из ответа `/users/me/blocks` теперь хранятся в
   разделе, а не добываются из реестра пользователей, где их могло не быть.
   Если идентификатора нет и там — показывается ошибка, строка остаётся,
   запрос не подделывается успехом. Проверено на сборке подменой ответа:
   без числового id `DELETE` не уходит, строка на месте, тост — ошибка;
   с числовым id уходит `DELETE /users/4242/block`, и строка исчезает
   только после ответа (1 → 0).

   Ниже — как было записано.

   **Разблокировка может не дойти до сервера.** Если у пользователя в списке
   нет числового id, запрос не отправляется, а тост «…разблокирован»
   показывается. `BlockedUsersSection.tsx:52-61`.
4. ~~**«Очистить историю» при ошибке сервера показывает пустой список**~~ —
   **починено 12.09**. `catch` больше не повторяет `try`: список пустеет
   только после ответа сервера, при отказе показывается ошибка с его
   сообщением, а локальная копия истории чистится вместе с серверной.
   Проверено на сборке подменой ответа на 500: список остался (32 ссылки до
   и после), показан тост «Не удалось очистить историю — Сервер не смог».

   Ниже — как было записано.

   **«Очистить историю» при ошибке сервера показывает пустой список** — после
   перезагрузки всё на месте. `settings.history.tsx:58-65`.
5. **Карта для выплат** — **проверено и уточнено 12.09.** Утверждение не
   подтвердилось в той части, где оно про реквизиты: поле карты там —
   `CardNumberInput`, и оно режет ввод до 16 цифр и набором, и вставкой.
   Замер на сборке: 19 цифр обоими способами дают «1234 5678 9012 3456»,
   ровно 16. Маска стоит там с 13.07 (`463b5ef8`) — за два месяца до аудита,
   то есть строка ниже была неверной с самого начала.

   Настоящее свободное поле — **получатель в окне вывода**: обычный `Input`
   с единственной проверкой «не пусто», а сервер держит `destination` просто
   строкой до 255 знаков. Починено там: при выводе на карту поле стало тем же
   `CardNumberInput` (второго не заводили), у счёта — 20 цифр, у СБП —
   телефон; смена способа очищает получателя, чтобы номер карты не уехал в
   реквизиты счёта. Ошибки внятные и по способу. Проверено: 19 цифр → 16;
   8 цифр → «Номер карты — ровно 16 цифр», запрос на сервер не уходит; смена
   способа очищает поле.

   Ниже — как было записано.

   **Карта для выплат:** форма пускает 16 цифр и больше, сервер принимает ровно
   16. На карту из 18–19 цифр — общая ошибка «Не удалось сохранить карту».
   `settings.requisites.tsx:200`, `UpdatePayoutRequisitesController.php:15`.
6. ~~**Ошибка загрузки выглядит как пустые данные**~~ — **починено 12.09**
   во всех шести местах. Заведён один общий блок
   `components/ui/load-failed.tsx` с теми же `errors.routeTitle` /
   `errors.routeDesc` / `errors.retry`, что у `RouteErrorState` и истории
   просмотров: шесть копий одного и того же городить было незачем, а три уже
   существовали. Отказ теперь не подставляет пустое значение — ни нули в
   «Статистике», ни пустой список карт, ни ноль в кошельке.

   Проверено на сборке подменой каждого ответа на 500, по одному разделу:

   | раздел | отказ показан | «Повторить» |
   | --- | --- | --- |
   | Статистика | да | есть |
   | Способы оплаты | да | есть |
   | Кошелёк | да | есть |
   | Мои обращения | да | есть |
   | Заблокированные | да | есть |
   | Карта для выплат | да | есть |

   Ниже — как было записано.

   **Ошибка загрузки выглядит как пустые данные:** «Статистика» — нули,
   «Способы оплаты» — «Пока нет привязанных карт», кошелёк — «0 ₽»,
   «Мои обращения», список заблокированных — «Никто не заблокирован», карта
   для выплат — сохранённая не видна.

## Работает неочевидно

7. ~~**Вывод денег без шага подтверждения.**~~ — **починено 12.09** в
   `fix/settings-obvious`. Перед отправкой показывается окно с суммой,
   способом и получателем: сервер списывает сразу и отменить заявку
   пользователь не может. Сохранённая карта подставляется — полный номер
   лежит на сервере (`user_payout_requisites.payout_card_number`), наружу
   по-прежнему уходят только последние четыре, а клиент шлёт
   `use_saved_card: true`, и получателя заполняет сервер. Закреплено
   `tests/Feature/WithdrawSavedCardTest.php` (5 проверок): флаг подставляет
   номер, без сохранённой карты — 422 `saved_card_missing`, для СБП и счёта —
   422 `saved_card_wrong_method`, без флага получатель по-прежнему обязателен,
   явный номер работает как раньше. Проверено на сборке: при сохранённой карте
   появляется выбор и поле номера скрыто, без неё выбора нет; отмена
   подтверждения не отправляет ни одного запроса на вывод.

   Ниже — как было записано.

   **Вывод денег без шага подтверждения.** Реквизиты — свободным текстом,
   сохранённая в «Реквизитах» карта не подставляется; сервер списывает сразу.
8. ~~**Без подтверждения**~~ — **починено 12.09** в `fix/settings-obvious`.
   Все три действия спрашивают через `askConfirm` — механизм в проекте уже
   был. Вторая половина серьёзнее: `AccountSecurityService::logoutOtherDevices`
   молча выходил, если текущего токена нет, а контроллер в обоих случаях
   отвечал `ok`. Теперь метод возвращает число завершённых сеансов, ответ
   несёт `ended_sessions`, и страница говорит по нему: «Завершено 3 сеанса»
   либо «Других сеансов не было — завершать нечего». Закреплено
   `tests/Feature/LogoutOtherSessionsTest.php` (3 проверки, включая то, что
   текущий сеанс переживает вызов). Проверено на сборке: у всех трёх действий
   окно подтверждения появляется, отмена не показывает ни одного тоста.

   Ниже — как было записано.

   **Без подтверждения:** «Выйти на других устройствах» (и тост об успехе,
   даже когда сервер ничего не сделал), «Очистить историю», «Очистить все» в
   уведомлениях.
9. ~~**Кошелёк открывается без подтверждённого телефона**~~ — **разобрано и
   починено 12.09**, вместе с пунктом 10. Утверждение верное, и комментарий в
   коде врал: `route.settings` объявлен в карте доступа `auth`, а
   `levelFromAccessTier("auth")` возвращает `registered`, не `verified`.
   Комментарий переписан по факту.

   Гейтом это не закрывали намеренно: поднять весь `/settings` до `verified`
   нельзя — телефон подтверждают на этой же странице, и человек заперся бы
   снаружи. Вместо этого предупреждение показывается **до** действия:
   `VerificationBanner` добавлен в кошелёк, реквизиты, способы оплаты и к
   списку заблокированных (снятие блокировки сервер держит за `verified` —
   `DELETE /users/{id}/block` в группе с этим посредником, сам список читается
   с `auth`).

   Проверено на сборке по ступеням: гость — баннера нет ни на одной из
   четырёх страниц, зарегистрированный без SMS — баннер на всех четырёх,
   подтверждённый — нет нигде.

   Ниже — как было записано.

   **Кошелёк открывается без подтверждённого телефона**, хотя комментарий в
   коде говорит обратное; окно подтверждения появляется только по нажатию
   «Пополнить» или «Вывести». `settings.wallet.tsx:45-64`.
10. ~~**Запись в реквизиты, карту выплат, карты и разблокировка требуют
    подтверждённого телефона**, но раздел не предупреждает~~ — **починено
    12.09** вместе с пунктом 9, тем же баннером.

    Ниже — как было записано.

    **Запись в реквизиты, карту выплат, карты и разблокировка требуют
    подтверждённого телефона**, но раздел об этом не предупреждает — только
    ошибка после нажатия.
11. ~~**SMS:** повтор через жёстко заданные 60 с~~ — **починено 12.09**.
    Паузу называет сервер: при успехе берётся `resend_after` из ответа, при
    отказе — `retry_after` через `readSmsRefusal`, который был написан, но не
    вызывался. Отказ оператора кнопку не держит: срока у него нет, ждать
    бесполезно — надо исправить номер. Проверено на сборке подменой ответа:
    `resend_after: 137` → «Повторить через 136 сек», кнопка заблокирована;
    `retry_after: 245` → «Повторить через 244 сек», заблокирована;
    `sms_provider_rejected` без срока → кнопка остаётся доступной.

    Ниже — как было записано.

    **SMS:** повтор через жёстко заданные 60 с; серверные `resend_after` и
    отказы не учитываются, хотя разбор для них есть (`readSmsRefusal`).
12. ~~**MAX:** «Дублировать уведомления в MAX» в двух местах~~ —
    **починено 12.09**. Проверено, что это буквально одна настройка: оба
    переключателя звали `saveMaxChannelPref`. Оставлен один — в
    «Уведомлениях», где живут остальные переключатели уведомлений; карточке
    MAX в «Профиле» остались привязка и отвязка, а на её месте — строка со
    ссылкой в «Уведомления». Проверено на сборке при привязанном MAX:
    в «Профиле» переключателей 0 и есть ссылка, в «Уведомлениях» — 1.

    Ниже — как было записано.

    **MAX:** «Дублировать уведомления в MAX» есть и в «Профиле», и в
    «Уведомлениях» — это одна настройка в двух местах.
13. **Тема** сохраняется только на этом устройстве.
14. ~~**«Экспорт моих данных»** может не скачаться~~ — **починено 12.09**.
    Ссылка теперь вносится в документ перед щелчком (Firefox не открывает
    `click()` у элемента вне дерева) и адрес отзывается не сразу, а следующей
    задачей — прежний `revokeObjectURL` сразу после щелчка успевал отобрать
    адрес раньше, чем браузер начинал скачивание. Тост больше не обещает
    сохранённый файл: «Файл с данными подготовлен — проверьте загрузки».
    Проверено на сборке: скачивание начинается, имя файла
    `modelizm-data-export-2026-09-12.json`.

    Ниже — как было записано.

    **«Экспорт моих данных»** может не скачаться в части браузеров: ссылка на
    файл отзывается сразу после щелчка.
15. ~~**Заглушки при готовом сервере**~~ — **разобрано и починено 12.09**.

    «Динамика просмотров» нарисована: 30 дней своим `svg` — графической
    библиотеки в проекте нет, и заводить её ради одной ломаной незачем.
    Попутно нашлось, почему заглушка держалась так долго: клиентская функция
    `fetchViewsDaily` разбирала ответ как `{ data: [...] }`, а маршрут отдаёт
    **голый массив** (проверено на проде: `[{"date":"2026-08-14","count":0},…]`),
    то есть вернула бы пустоту — заметить это было негде, её никто не вызывал.
    Теперь принимаются обе формы. Высота поля графика фиксирована, поэтому
    после загрузки блок не меняет размер: замер на сборке — сдвиг 0,0006 во
    всех трёх состояниях. Нули показываются честно («За 30 дней просмотров не
    было»), отказ — с «Повторить».

    Про ответ на отзыв запись была **неверной**: он работает —
    `replyToUserReview` вызывается в `ProfileView` на вкладке «Отзывы».
    Второго такого же экрана в настройках не заводили; вместо него — ссылка
    ровно на ту вкладку. Чтобы ссылка вела не в никуда, вкладка профиля стала
    открываться хешем (`/profile#reviews`) — тем же приёмом, что
    `#sms-verify` в настройках. Проверено: переход открывает вкладку
    «Отзывы», она активна.

    Ниже — как было записано.

    **Заглушки при готовом сервере:** «Динамика просмотров» — «появится
    позже», хотя данные по дням сервер отдаёт (`/users/me/stats/views-daily`);
    в «Рейтинге» нельзя ответить на отзыв, хотя сервер умеет
    (`POST /users/me/reviews/{uuid}/reply`).
16. **Мелочи:** «Мои согласия» — текст мимо переводов; «Мой канал и
    сообщество» — на телефоне нет ссылки «Настройки» назад.
